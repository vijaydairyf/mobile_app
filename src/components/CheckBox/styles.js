import {dpToPx} from "../../utils/utility";

const styles = {
    termsPolicyTextStyle: {
        paddingLeft: dpToPx(3),
        fontSize: dpToPx(6),
        color: "#16c2bf"
    },
    checkboxCont: {
        flexDirection: "row"
    }
};

export default styles;
