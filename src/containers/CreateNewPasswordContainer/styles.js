import {dpToPx} from "../../utils/utility";

const styles = {
    appContainer: {
        flex: 1,
        backgroundColor: "#ffffff"
    },
    passwordStrength:{
        color:"#50BCBA"
    }
};

export default styles;
